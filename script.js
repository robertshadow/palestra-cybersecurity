const MATERIAL_REAL_URL="https://example.org/material-real";function show(id){document.querySelectorAll(".card").forEach(x=>x.classList.remove("active"));document.getElementById(id).classList.add("active");scrollTo(0,0)}document.querySelectorAll("[data-next]").forEach(b=>b.onclick=()=>show(b.dataset.next));document.getElementById("real").onclick=()=>{if(MATERIAL_REAL_URL.includes("example.org"))alert("O material real ainda não foi configurado pelo palestrante.");else location.href=MATERIAL_REAL_URL};